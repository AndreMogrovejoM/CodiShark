import { render } from "setupTests";

import AdminClientListView from "./AdminClientListView";

describe("AdminClientListView", () => {
  it("renders with default props", () => {
    render(<AdminClientListView />);
  });
});
