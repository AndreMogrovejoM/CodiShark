import { render } from "setupTests";

import IndicatorsComponent from "./Indicators";

describe("IndicatorsComponent", () => {
  it("renders with default props", () => {
    render(<IndicatorsComponent />);
  });
});
