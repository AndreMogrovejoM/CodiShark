import { render } from "setupTests";

import ButtonRowsComponent from "./ButtonRowsComponent";

describe("ButtonRowsComponent", () => {
  it("renders with default props", () => {
    render(<ButtonRowsComponent />);
  });
});
