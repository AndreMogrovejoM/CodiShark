import { render } from "setupTests";

import InfoCardClient from "./InfoCardClient";

describe("InfoCardClient", () => {
  it("renders with default props", () => {
    render(<InfoCardClient />);
  });
});
