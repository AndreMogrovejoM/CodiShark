import React from "react";
import { render } from "setupTests";

import SignInAdministrator from "./SignInAdministrator";

describe("SignInAdministrator", () => {
  it("renders with default props", () => {
    render(<SignInAdministrator />);
  });
});
