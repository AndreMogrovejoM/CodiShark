import styled from "styled-components";

import { BackdropStyledProps as Props } from "./Backdrop.types";

const BackdropStyled = styled.div<Props>`
  .Backdrop {
  }
`;

export default BackdropStyled;
