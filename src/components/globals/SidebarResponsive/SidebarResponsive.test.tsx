import { render } from "setupTests";

import SidebarResponsive from "./SidebarResponsive";

describe("SidebarResponsive", () => {
  it("renders with default props", () => {
    render(<SidebarResponsive />);
  });
});
