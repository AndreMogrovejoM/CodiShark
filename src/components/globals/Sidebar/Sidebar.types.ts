// Interfaces and types from component Sidebar

// Component Props
export interface SidebarProps {
  open?: boolean;
  onClose?: () => void;
}

// Styled Component Props
export interface SidebarStyledProps {
  className: string;
}
