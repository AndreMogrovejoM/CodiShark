import { render } from "setupTests";

import PaymentSucces from "./PaymentSucces";

describe("PaymentSucces", () => {
  it("renders with default props", () => {
    render(<PaymentSucces />);
  });
});
