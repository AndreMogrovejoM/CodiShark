import { render } from "setupTests";

import InformationClient from "./InformationClient";

describe("InformationClient", () => {
  it("renders with default props", () => {
    render(<InformationClient />);
  });
});
