import { render } from "setupTests";

import PaymentRejected from "./PaymentRejected";

describe("PaymentRejected", () => {
  it("renders with default props", () => {
    render(<PaymentRejected />);
  });
});
