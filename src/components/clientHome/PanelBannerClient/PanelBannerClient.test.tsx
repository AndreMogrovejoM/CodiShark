import { render } from "setupTests";

import PanelBannerClient from "./PanelBannerClient";

describe("PanelBannerClient", () => {
  it("renders with default props", () => {
    render(<PanelBannerClient />);
  });
});
