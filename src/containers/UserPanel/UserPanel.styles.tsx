import styled from "styled-components";

import { UserPanelStyledProps as Props } from "./UserPanel.types";

const UserPanelStyled = styled.div<Props>`
  .UserPanel {
  }
`;

export default UserPanelStyled;
