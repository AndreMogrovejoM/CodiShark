import { render } from "setupTests";

import SetupProvider from "./SetupProvider";

describe("SetupProvider", () => {
  it("renders with default props", () => {
    render(<SetupProvider />);
  });
});
