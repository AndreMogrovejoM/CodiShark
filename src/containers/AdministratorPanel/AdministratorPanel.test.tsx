import { render } from "setupTests";

import AdministratorPanelContainer from "./AdministratorPanel";

describe("AdministratorPanelContainer", () => {
  it("renders with default props", () => {
    render(<AdministratorPanelContainer />);
  });
});
